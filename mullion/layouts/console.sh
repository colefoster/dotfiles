# Console — a status line, not a bar. Workspaces read as [1:gh zd], fields are
# divided by rules rather than boxed, and everything is on screen at once.
LAYOUT_NAME="Console"
BAR_POSITION=top
BAR_ISLANDS=off
BAR_TRANSPARENT=off
PIP_MODE=icons
PIP_FORM=bracket        # number | icons | bracket
CPU_FORM=text           # text | graph
HIDE_EMPTY=off
KEEP_NATIVE_MENUBAR=off
ANIMATE=off
ITEMS_LEFT="spaces front_app"
ITEMS_RIGHT="net claude cpu battery clock"
ITEMS_CENTER=""
