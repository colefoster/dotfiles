# Dock — one centred pill at the foot holding workspaces and nothing else.
# macOS keeps its own menu bar, so the clock and battery stay where they were.
LAYOUT_NAME="Dock"
BAR_POSITION=bottom
BAR_ISLANDS=on
BAR_TRANSPARENT=on
PIP_MODE=icons
PIP_FORM=icons
CPU_FORM=text
HIDE_EMPTY=on
KEEP_NATIVE_MENUBAR=on
ANIMATE=on
ITEMS_LEFT=""
ITEMS_RIGHT=""
ITEMS_CENTER="spaces"
